# TODO 0902：数据加载边界收尾

## 1. 统一非 SmartQuant Source 的规范化入口（已完成）

- 让 `MemoryDataProvider` 和仓库因子加载也通过 `normalize_batches` 进入 Runtime。
- 确保所有 `DataProvider.load_many()` 返回值遵守相同的 `float64`、`T × N × S`、只读、Infinity 和 ValueKind 契约。
- 增加非法 Infinity、MASK、CODE 和 shape 的回归测试。

## 2. 校验显式 default（已完成）

- 显式 default 应与物理数据使用同一套 Infinity 和 ValueKind 校验。
- NUMERIC 的正负 Infinity 转为 NaN。
- MASK 只允许 `0/1/NaN`，CODE 只允许整数或 NaN。
- 增加非法 MASK/CODE default 和 Infinity default 测试。

## 3. 跨 RawBatch 检查 labels/static 重复坐标（已完成）

- `labels` 和 `static` 模式应在整个 RawBatch 流范围内跟踪已占用坐标，而不只检查单个批次。
- 重复判断应基于解析、规范化后的坐标，防止不同原始表示映射到同一位置后被静默覆盖。
- 增加同批次、跨批次和规范化后重复的测试。

## 4. 整合转债正股映射 Source（已完成）

- 整合 `data_sources.json` 中的 `cb.1d.underlying_stk_col` 与 `cb.1d.underlying_stk_inner_code`。
- 明确统一 Source 如何表达 `axis_position` 与 `inner_code` 两种用途，避免重复的数据源定义和读取逻辑。
- 保持现有股票到转债投影和原始 `StockInnerCode` 读取能力，并补充兼容性与 LoadGroup 测试。
